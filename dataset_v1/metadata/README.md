# Dataset V1 Metadata

This directory is reserved for repository-safe Dataset V1 metadata and documentation.

Protected REAL audio is intentionally excluded from Git. Metadata files should only be committed after confirming that they contain no sensitive speaker information and that their provenance permits repository publication.

The protected REAL master manifest remains authoritative for the working dataset.
