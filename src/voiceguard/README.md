# VoiceGuard Core Package

This directory is reserved for the importable VoiceGuard application and ML components as they are migrated from the verified working project.

Only tested, real project code should be added here. Dataset-specific paths, protected audio, local environments, and generated checkpoints do not belong in this package.
